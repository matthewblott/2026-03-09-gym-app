<% content_for :title, 'New Exercise' %>

<style>
  label {
    span {
      margin-left: 0.5rem;
    }
    input, select {
      margin-top: 0.5rem;
    }
  }

  .visually-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  #search_results {
    padding: 0;
    margin: 0;
  }

  [role="radiogroup"] { 
    padding-top: 0;
    padding-bottom: 0;
    margin-top: 0;
    margin-bottom: 0;
  }

</style>

<% content_for :header_controls do %>
  <form
    id="workout-exercise-form"
    method="post"
    action="<%= user_create_workout_exercise_path(Current.user, workout_id: @workout_id) %>"
  >
    <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
    <input type="hidden" name="workout_exercise[workout_id]" value="<%= @workout_id %>">
  </form>
  <div role=group>
    <a href="<%= user_workout_exercises_path(Current.user, workout_id: @workout_id) %>" role="button" data-controller="bridge--button">Exercises</a>
    <button form="workout-exercise-form" data-controller="bridge--button">Save</button>
  </div>
<% end %>

<div class="mt-m">
</div>

<form action="<%= user_search_exercises_path %>" id="search-form" method="post"></form>

<fieldset data-controller="search-field mirror-field" id="message">
  <div>
    <label>
      <span>Name</span>
      <input
        type="search"
        name="q"
        placeholder="Search or add new"
        data-search-field-target="query"
        form="search-form"
        data-mirror-field-target="source"
        data-action="input->search-field#search input->mirror-field#sync"
        required
      >
      <button
        form="search-form"
        type="submit"
        data-search-field-target="submit"
        class="hidden"
      >
      </button>
      <input
        placeholder="name"
        form="workout-exercise-form"
        name="workout_exercise[name]"
        data-search-field-target="text"
        data-mirror-field-target="hidden"
        class="visually-hidden"
        required
      >
      <input
        type="hidden"
        placeholder="id"
        form="workout-exercise-form"
        name="workout_exercise[exercise_id]"
        data-search-field-target="value"
      >
    </label>
  </div>
  <div
    id="search_results"
    data-controller="search-results"
    data-search-results-search-field-outlet="#message"
    data-search-results-target="container"
  >
  </div>
  <div role="radiogroup">
    <span>Exercise Type</span>
    <label>
      <input
        form=workout-exercise-form
        type="radio"
        name="workout_exercise[exercise_type]"
        data-search-field-target="cardio"
        value="cardio"
        required
      >
      Cardio
    </label>
    <label>
      <input
        form=workout-exercise-form
        type="radio"
        name="workout_exercise[exercise_type]"
        data-search-field-target="weights"
        value="weights"
      >
      Weights
    </label>
  </div>
</fieldset>
