<% content_for :title, "New Workout" %>

<section>
  <h1>New workout</h1>

  <%= render "form", workout: @workout %>

  <p><a href="<%= workouts_path(Current.user) %>">Back to workouts</a></p>
</section>
